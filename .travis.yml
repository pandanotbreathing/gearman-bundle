language: php
php:
  - hhvm
  - 5.6
  - 7.0

install:
  - composer install --prefer-dist

script:
  - vendor/bin/phpcs --standard=psr2 --extensions=php {$TRAVIS_BUILD_DIR}/
  - vendor/bin/phpunit {$TRAVIS_BUILD_DIR}/

matrix:
  allow_failures:
    - php: hhvm