language: php
php:
  - hhvm
  - 5.6
  - 7.0

install:
  - composer install --prefer-dist

script:
  - vendor/bin/phpcs --standard=psr2 --extensions=php ./
  - vendor/bin/phpunit ./

matrix:
  allow_failures:
    - php: hhvm